[pytest]
# Test discovery
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Async mode for pytest-asyncio
asyncio_mode = auto

# Add backend/ to PYTHONPATH so imports resolve correctly
pythonpath = .

# Verbosity
addopts = -v --tb=short
